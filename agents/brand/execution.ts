import type { BrandGeneration,BrandGenerationInput,BrandProvider } from "./types";
export interface BrandRefs{runId:string;jobId:string;businessId:string;selectedProductId:string}
export interface BrandPersistence{start(input:BrandGenerationInput,provider:string,model:string):Promise<BrandRefs>;complete(refs:BrandRefs,generation:BrandGeneration):Promise<void>;fail(refs:BrandRefs,error:string):Promise<void>}
export async function executeBrandGeneration(input:BrandGenerationInput,providerName:string,model:string,provider:BrandProvider,persistence:BrandPersistence){const refs=await persistence.start(input,providerName,model);try{const generation=await provider.generate(input);await persistence.complete(refs,generation);return{...refs,directions:generation.directions}}catch(error){const message=error instanceof Error?error.message:"Unknown brand generation failure";try{await persistence.fail(refs,message)}catch(cleanup){console.error("Brand generation cleanup failed",{refs,cleanup})}throw error}}
