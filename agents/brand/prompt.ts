import type { BrandGenerationInput } from "./types";
export function buildBrandPrompt(i:BrandGenerationInput){return `Create ${i.directionCount} distinct ecommerce brand directions. Business: ${i.businessName}. Country: ${i.targetCountry}. Market: ${i.targetMarket}. Budget: ${i.startingBudget}. Niche: ${i.niche}. Selected product: ${i.productName} (${i.productCategory}). Description: ${i.productDescription}. Customer: ${i.productTargetCustomer}. Price: ${i.suggestedPrice}. Marketing angles: ${i.marketingAngles.join(", ")}. Risks: ${i.risks.join(", ")}. Research reasoning: ${i.reasoning}. Optional preferences: ${JSON.stringify(i.preferences)}. Avoid trademarks, competitor names, unreadable names, misleading location claims, guaranteed outcomes, and unsafe claims. Domain and handle ideas are suggestions only; do not claim availability or legal clearance. Return structured JSON only.`}
