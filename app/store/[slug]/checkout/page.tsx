import{notFound}from"next/navigation";import{getPublicCheckout}from"@/lib/data";import{formatMinorUnits,multiplyMinorUnits}from"@/providers/payments/money";import{submitCheckout}from"@/lib/actions/checkout";
export default async function CheckoutPage({params,searchParams}:{params:Promise<{slug:string}>;searchParams:Promise<{error?:string}>}){const{slug}=await params;const{error}=await searchParams;const data=await getPublicCheckout(slug);if(!data)notFound();const mock=(process.env.PAYMENT_PROVIDER||"mock")==="mock";return <main className="mx-auto max-w-3xl px-6 py-12"><a href={`/store/${slug}`} className="text-sm underline">← Back to {data.store.name}</a><h1 className="mt-6 text-3xl font-bold">Checkout</h1>{mock&&<div className="mt-5 rounded-xl border border-amber-300 bg-amber-50 p-4 text-amber-900"><strong>Test Payment</strong><p>No real payment will be charged.</p></div>}{error&&<p className="mt-5 rounded-lg bg-red-50 p-3 text-red-700">{error}</p>}<section className="mt-6 rounded-2xl border p-5"><h2 className="font-bold">{data.product.name}</h2><p className="mt-2 text-sm opacity-70">{data.product.description}</p><p className="mt-3 font-semibold">{formatMinorUnits(data.product.unitPrice,data.product.currency)} each</p></section><form action={submitCheckout} className="mt-6 grid gap-4 rounded-2xl border p-6 md:grid-cols-2"><input type="hidden" name="slug" value={slug}/><label>Quantity<input className="mt-1 w-full rounded-lg border p-3" name="quantity" type="number" min="1" max="10" defaultValue="1" required/></label><div className="self-end text-sm">Shipping: {formatMinorUnits(data.shippingAmount,data.product.currency)} · Tax: {formatMinorUnits(data.taxAmount,data.product.currency)}</div><label>First name<input className="mt-1 w-full rounded-lg border p-3" name="firstName" required/></label><label>Last name<input className="mt-1 w-full rounded-lg border p-3" name="lastName" required/></label><label>Email<input className="mt-1 w-full rounded-lg border p-3" name="email" type="email" required/></label><label>Phone (optional)<input className="mt-1 w-full rounded-lg border p-3" name="phone"/></label><label>Country code<input className="mt-1 w-full rounded-lg border p-3 uppercase" name="countryCode" defaultValue="UG" maxLength={2} required/></label><label>Address line 1<input className="mt-1 w-full rounded-lg border p-3" name="addressLine1" required/></label><label>Address line 2 (optional)<input className="mt-1 w-full rounded-lg border p-3" name="addressLine2"/></label><label>City<input className="mt-1 w-full rounded-lg border p-3" name="city" required/></label><label>Region or state<input className="mt-1 w-full rounded-lg border p-3" name="region" required/></label><label>Postal code (optional)<input className="mt-1 w-full rounded-lg border p-3" name="postalCode"/></label>{mock&&<label>Test scenario<select className="mt-1 w-full rounded-lg border p-3" name="scenario"><option value="success">Mock success</option><option value="failure">Mock failure</option><option value="cancel">Mock cancellation</option></select></label>} {!mock&&<input type="hidden" name="scenario" value="success"/>}<div className="md:col-span-2 rounded-lg bg-slate-50 p-4"><p>Subtotal for 1: {formatMinorUnits(multiplyMinorUnits(data.product.unitPrice,1),data.product.currency)}</p><p className="text-xs opacity-70">Taxes calculated according to current store configuration. Shipping is currently free.</p></div><button className="md:col-span-2 rounded-lg bg-black px-5 py-3 font-semibold text-white">{mock?"Complete Test Payment":"Continue to payment"}</button></form></main>}
