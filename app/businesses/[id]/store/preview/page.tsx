import{notFound}from"next/navigation";import Link from"next/link";import{getStoreOverview}from"@/lib/data";import{StoreRenderer}from"@/components/store-renderer";export default async function StorePreview({params}:{params:Promise<{id:string}>}){const{id}=await params;const data=await getStoreOverview(id);if(!data.store)notFound();return <div><div className="bg-slate-950 px-6 py-3 text-center text-sm text-white">Private preview · <Link className="underline" href={`/businesses/${id}/store`}>Return to editor</Link></div><StoreRenderer store={data.store} sections={data.sections} testData={data.run?.provider==="mock"}/></div>}
