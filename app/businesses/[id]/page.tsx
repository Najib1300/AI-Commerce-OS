import { notFound } from "next/navigation";
import { CheckCircle2,LockKeyhole,Search } from "lucide-react";
import { Badge,Card } from "@/components/ui";
import { getBusiness } from "@/lib/data";
import { formatCurrency } from "@/lib/utils";
const workflows=["Product Research","Brand Creation","Store Creation","SEO","Marketing Assets","Launch"];
export default async function BusinessPage({params}:{params:Promise<{id:string}>}){const {id}=await params;const business=await getBusiness(id);if(!business)notFound();const details=[["Target country",business.target_country],["Target market",business.target_market],["Starting budget",formatCurrency(business.starting_budget)],["Niche",business.niche]];return <div><div className="mb-7"><div className="flex items-center gap-3"><h1 className="text-3xl font-bold">{business.name}</h1><Badge tone="warning">{business.status}</Badge></div><p className="mt-2 text-muted">Dropshipping business overview</p></div><Card className="mb-7"><h2 className="font-semibold">Business brief</h2><dl className="mt-5 grid gap-5 sm:grid-cols-2 lg:grid-cols-4">{details.map(([label,value])=><div key={label}><dt className="text-xs font-medium uppercase tracking-wide text-muted">{label}</dt><dd className="mt-1 font-semibold">{value}</dd></div>)}</dl></Card><h2 className="mb-4 text-xl font-bold">Build workflow</h2><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{workflows.map((name,index)=><Card key={name} className={index===0?"border-brand-500":""}><div className="flex items-start justify-between"><span className={index===0?"grid size-10 place-items-center rounded-xl bg-brand-50 text-brand-600":"grid size-10 place-items-center rounded-xl bg-slate-100 text-slate-400"}>{index===0?<Search size={19}/>:<LockKeyhole size={18}/>}</span>{index===0?<Badge tone="success">Ready</Badge>:<Badge>Locked</Badge>}</div><h3 className="mt-4 font-semibold">{name}</h3><p className="mt-2 text-sm text-muted">{index===0?"Ready for the next development phase.":"Complete earlier workflow steps to unlock."}</p>{index===0&&<div className="mt-4 flex items-center gap-2 text-xs font-medium text-emerald-700"><CheckCircle2 size={15}/>Phase 1B entry point</div>}</Card>)}</div></div>}
