import { Button,Card,Dropdown,Input } from "@/components/ui";
import { createBusiness } from "@/lib/actions/businesses";
import { nicheOptions } from "@/lib/validation";
export default async function NewBusinessPage({searchParams}:{searchParams:Promise<{error?:string}>}){const {error}=await searchParams;return <div className="mx-auto max-w-2xl"><div className="mb-7"><p className="text-sm font-medium text-brand-600">New business</p><h1 className="mt-1 text-3xl font-bold">Create your business</h1><p className="mt-2 text-muted">Define the starting brief. You can refine it later.</p></div><Card>{error&&<p className="mb-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}<form action={createBusiness} className="grid gap-5"><label className="grid gap-2 text-sm font-medium">Business name<Input name="name" required/></label><div className="grid gap-5 sm:grid-cols-2"><label className="grid gap-2 text-sm font-medium">Target country<Input name="targetCountry" placeholder="United States" required/></label><label className="grid gap-2 text-sm font-medium">Target selling market<Input name="targetMarket" placeholder="North America" required/></label></div><label className="grid gap-2 text-sm font-medium">Starting budget (USD)<Input name="startingBudget" type="number" min="0" step="0.01" required/></label><label className="grid gap-2 text-sm font-medium">Niche<Dropdown name="niche" required>{nicheOptions.map(option=><option key={option}>{option}</option>)}</Dropdown></label><Button type="submit" className="mt-2">Create Business</Button></form></Card></div>}
