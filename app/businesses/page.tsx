import Link from "next/link";
import { Plus } from "lucide-react";
import { Badge,Card,EmptyState } from "@/components/ui";
import { getBusinesses } from "@/lib/data";
export default async function BusinessesPage(){const businesses=await getBusinesses();return <div><div className="mb-7 flex items-end justify-between"><div><h1 className="text-3xl font-bold">My Businesses</h1><p className="mt-2 text-muted">Manage every commerce business in your workspace.</p></div><Link href="/businesses/new" className="inline-flex items-center gap-2 rounded-lg bg-brand-500 px-4 py-2.5 text-sm font-semibold text-white"><Plus size={17}/>Create Business</Link></div>{businesses.length===0?<EmptyState title="No businesses yet" description="Create your first business to start the workflow."/>:<div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{businesses.map(item=><Link href={`/businesses/${item.id}`} key={item.id}><Card className="transition hover:-translate-y-0.5 hover:shadow-md"><div className="flex items-start justify-between"><h2 className="text-lg font-semibold">{item.name}</h2><Badge>{item.status}</Badge></div><p className="mt-5 text-sm text-muted">{item.niche} · {item.target_market}</p></Card></Link>)}</div>}</div>}
