export function ProductScore({label,value,inverted=false}:{label:string;value:number;inverted?:boolean}){const quality=inverted?100-value:value;const color=quality>=70?"bg-emerald-500":quality>=45?"bg-amber-500":"bg-red-500";return <div><div className="mb-1 flex justify-between text-xs"><span className="text-muted">{label}</span><span className="font-semibold">{Math.round(value)}</span></div><div className="h-1.5 overflow-hidden rounded-full bg-slate-100"><div className={`h-full ${color}`} style={{width:`${Math.min(100,Math.max(0,value))}%`}}/></div></div>}
