import Link from "next/link";
import { Button,Input } from "@/components/ui";
export function AuthFrame({title,subtitle,error,children}:{title:string;subtitle:string;error?:string;children:React.ReactNode}){return <main className="grid min-h-screen place-items-center bg-slate-50 p-5"><div className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-7 shadow-sm"><Link href="/" className="mb-7 block font-bold text-brand-600">AI Commerce OS</Link><h1 className="text-2xl font-bold">{title}</h1><p className="mt-2 text-sm text-muted">{subtitle}</p>{error&&<p className="mt-4 rounded-lg bg-red-50 p-3 text-sm text-red-700">{error}</p>}{children}</div></main>}
export function Field({label,name,type="text",placeholder,autoComplete,required=true}:{label:string;name:string;type?:string;placeholder?:string;autoComplete?:string;required?:boolean}){return <label className="grid gap-2 text-sm font-medium">{label}<Input name={name} type={type} placeholder={placeholder} autoComplete={autoComplete} required={required}/></label>}
export function Submit({children,disabled=false}:{children:React.ReactNode;disabled?:boolean}){return <Button type="submit" className="w-full" disabled={disabled}>{children}</Button>}
