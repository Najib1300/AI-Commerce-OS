import{z}from"zod";
export const checkoutInputSchema=z.object({slug:z.string().regex(/^[a-z0-9]+(?:-[a-z0-9]+)*$/).max(120),quantity:z.coerce.number().int().min(1).max(10),firstName:z.string().trim().min(1).max(80),lastName:z.string().trim().min(1).max(80),email:z.string().trim().toLowerCase().email().max(320),phone:z.string().trim().max(32).optional().default(""),countryCode:z.string().trim().toUpperCase().regex(/^[A-Z]{2}$/),addressLine1:z.string().trim().min(2).max(200),addressLine2:z.string().trim().max(200).optional().default(""),city:z.string().trim().min(1).max(100),region:z.string().trim().min(1).max(100),postalCode:z.string().trim().max(32).optional().default(""),scenario:z.enum(["success","failure","cancel"]).default("success")}).strict();
export const verifiedPaymentSchema=z.object({valid:z.boolean(),status:z.enum(["succeeded","failed","cancelled"]),provider:z.literal("mock"),transactionId:z.string().regex(/^mock_tx_[a-f0-9-]{36}$/),eventId:z.string().regex(/^mock_evt_[a-f0-9-]{36}$/),checkoutId:z.uuid(),paymentToken:z.uuid(),amount:z.number().int().nonnegative(),currency:z.string().regex(/^[A-Z]{3}$/),payloadHash:z.string().regex(/^[a-f0-9]{64}$/),metadata:z.record(z.string(),z.unknown())}).strict();
